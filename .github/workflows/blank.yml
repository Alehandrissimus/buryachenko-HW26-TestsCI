name: CI

on:
  pull_request:
    branches: [ Tests+CI ]

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: echo Hello, world!

      - name: Running tests
        run: ./gradlew app:tests
